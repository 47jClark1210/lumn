import test from 'node:test';
// import React from 'react';
// import { render, screen, fireEvent } from '@testing-library/react';
// import Login from '../pages/Login';

test('Login renders login form and video background', async () => {
  // const { getByText, getByAltText } = render(<Login onLoginSuccess={() => {}} />);
  // assert.ok(getByText(/sign in with sso/i));
  // assert.ok(getByAltText(/solar eclipse logo/i));
});

test('Login calls onLoginSuccess when SSO button is clicked', async () => {
  // let called = false;
  // const onLoginSuccess = () => { called = true; };
  // const { getByRole } = render(<Login onLoginSuccess={onLoginSuccess} />);
  // const button = getByRole('button', { name: /sign in with sso/i });
  // fireEvent.click(button);
  // assert.ok(called);
});
